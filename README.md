# RUAL
Ramp Up Active Learning (RUAL) for rapid virtual screening on enumerated libraries. The package utilizes Scikit-Learn models to prioritize molecules for subsequent rounds of scoring. The package has built in support for scoring with SMINA, but is also compatible with custom scoring functions.

